mutagen>=1.47.0
Pillow>=10.0.0
tkinterdnd2>=0.3.0

# Spotify Sync tab
spotipy>=2.24.0
requests>=2.31.0
# yt-dlp breaks periodically when YouTube changes its site — keep this loosely
# pinned and bump it if the Spotify Sync tab starts failing to find/download
# tracks. (`pip install -U yt-dlp`)
yt-dlp>=2024.8.6
